replicaCount: 1

image:
  repository: bankapp-exchange-generator
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8005

serviceAccount:
  create: true
  automount: true
  annotations: { }
  name: ""

ingress:
  enabled: true
  className: "nginx"
  annotations: { }
  hosts:
    - host: exchange-generator.test.local
      paths:
        - path: /
          pathType: Prefix
  tls: [ ]

environment:
  KAFKA_URL: kafka.kafka.svc.cluster.local:9092
  ZIPKIN_TRACING_ENDPOINT: "http://zipkin.{{ .Release.Namespace }}.svc.cluster.local:9411/api/v2/spans"
  ZIPKIN_SAMPLING_PROBABILITY: 1

resources: { }

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80